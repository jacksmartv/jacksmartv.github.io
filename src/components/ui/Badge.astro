---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-full border border-border bg-panel px-3 py-1 text-xs font-medium text-text-secondary',
    className,
  ]}
>
  <slot />
</span>
