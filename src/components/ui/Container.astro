---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article' | 'header' | 'footer' | 'main';
}

const { class: className, as: Tag = 'div' }: Props = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-content px-6 lg:px-10', className]}>
  <slot />
</Tag>
