---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-md bg-accent/10 px-2.5 py-1 font-mono text-xs text-accent',
    className,
  ]}
>
  <slot />
</span>
